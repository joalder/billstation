{
  "name": "Billstation",
  "description": "This app does not much yet, and does it well.",

  "repository": "https://github.com/joalder/billstation/",

  "buildpacks": [
    {
      "url": "heroku/python"
    },
    {
      "url": "heroku/nodejs"
    }
  ],

  "scripts": {
    "predeploy": "ln -s frontend/package.json . && ln -s frontend/node_modules .",
    "postdeploy": "postdeploy.sh"
  },

  "env": {
    "SECRET_TOKEN": {
      "description": "A secret key for verifying the integrity of signed cookies.",
      "generator": "secret"
    },
    "WEB_CONCURRENCY": {
      "description": "The number of processes to run.",
      "value": "1"
    }
  }
}