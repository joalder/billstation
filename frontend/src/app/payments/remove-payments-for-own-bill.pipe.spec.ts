import { RemovePaymentsForOwnBillPipe } from './remove-payments-for-own-bill.pipe';

describe('RemovePaymentsForOwnBillPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovePaymentsForOwnBillPipe();
    expect(pipe).toBeTruthy();
  });
});
